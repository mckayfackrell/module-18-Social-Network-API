// import the User and Thought models
const User = require('./User');
const Thought = require('./Thought');

// export the User and Thought models as a single object
module.exports = { User, Thought };